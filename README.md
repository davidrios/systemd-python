# Python apps in systemd

This repository contain helpers and instructions to run Python apps on Ubuntu 18.04 with systemd.

There are two folders, `gunicorn` and `uwsgi`, see each one for specific instructions.